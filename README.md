# 空中飞车 - 体感竞速

基于 WebGL 的未来风格 3D 竞速游戏，使用摄像头手势控制。

## 游戏特点

- 🎮 **体感控制**：使用摄像头识别手势，无需手柄或键盘
- 🚀 **未来风格**：科幻赛道和飞行器设计
- 🌐 **网页运行**：基于 WebGL，无需安装
- 👥 **双人对战**：局域网双人竞速模式

## 操作说明

| 手势 | 动作 |
|------|------|
| 双手握虚拟方向盘，左手低 | 左转 |
| 双手握虚拟方向盘，右手低 | 右转 |
| 右手拇指食指捏合 | 加速 |
| 右手张开 | 刹车 |

## 运行方法

### 单人模式

```bash
cd 项目目录
npx http-server . -p 3000
```

然后在浏览器访问：http://localhost:3000

### 双人模式 (局域网)

**主机 (创建房间):**
```bash
# 终端1: 启动游戏服务器
npx http-server . -p 3000

# 终端2: 启动 WebSocket 服务器
npm install  # 首次运行需要安装依赖
node server.js
```

**主机访问:** http://localhost:3000

**另一台电脑访问:** http://[主机IP]:3000
- 例如: http://192.168.1.149:3000

> ⚠️ **注意**: 非 localhost 访问时，浏览器可能阻止摄像头。需要在浏览器设置中允许：
> - Chrome: `chrome://flags/#unsafely-treat-insecure-origin-as-secure`
> - Edge: `edge://flags/#unsafely-treat-insecure-origin-as-secure`
> 
> 添加 `http://[主机IP]:3000` 并重启浏览器。

## 双人模式流程

1. 主机点击 "双人模式" → "创建房间"
2. 记下 6 位房间码
3. 另一玩家输入房间码 → "加入房间"
4. 双方点击 "准备"
5. 倒计时结束后开始比赛

## 技术栈

- Three.js (WebGL 渲染)
- MediaPipe (手势识别)
- WebSocket (多人同步)
- Node.js (服务器)

## 项目结构

```
AirRacer/
├── index.html          # 游戏入口
├── launch.js           # 启动逻辑
├── server.js           # 多人模式服务器
├── js/
│   ├── CameraInput.js      # 手势识别
│   └── MultiplayerClient.js # 多人客户端
├── bkcore/
│   ├── hexgl/              # 游戏核心
│   └── threejs/            # Three.js 扩展
└── textures/           # 纹理资源
```

## 致谢

本项目基于 [HexGL](https://github.com/BKcore/HexGL) 改造，原作者 Thibaut Despoulain (BKcore)。
